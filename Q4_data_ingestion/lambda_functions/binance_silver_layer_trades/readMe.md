This lambda_function.py file is used to create the lambda function which loads the raw trade data from landing zone and applies basic transformation to convert the data into tabular format and write to silver layer
